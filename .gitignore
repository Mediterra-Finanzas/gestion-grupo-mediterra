# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
jest.config.tmp.json

# production
/build

# misc
.DS_Store

# environment variables - NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.claude/

# Backups / snapshots con datos reales (emails, PINs) — NUNCA commitear
audit/*BACKUP*.json
audit/osiris-snapshot-*/
*.backup

# Wildcard para cualquier .env.X (notas, backups, variantes)
.env*

# Supabase CLI: cache local / temp del proyecto
supabase/.temp/

# Artefactos generados de informes/manuales (output local, no es codigo de la app)
# Los logos del grupo SI estan versionados en public/ (med.png, etc.)
Manual_*.pdf
gen_*.py
manual_preview-*.png
audit/gen_*.py

